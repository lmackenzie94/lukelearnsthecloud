{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "aws:deploy": "aws s3 sync dist/ s3://lukelearnsthe.cloud/ --delete",
    "aws:clear-cache": "aws cloudfront create-invalidation --distribution-id E2N6KLWY1L9MPP --paths '/*'"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}